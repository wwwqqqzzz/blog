## 安全快照
- 新建保护分支：`git switch -c chore/cleanup-2025-11-25`
- 全量保存当前工作区：`git add -A && git commit -m "chore: pre-clean snapshot"`
- 变更完成后再合并：先在该分支完成清理与验证，确认无误后再合并到主分支。

## 清理范围
- 目标：删除未被路由/主题/实际代码引用的页面、组件与冗余文件，减少体积与认知负担。
- 优先从 `src/pages` 与 `src/components` 清理，谨慎处理 `src/theme`、`api`、`data`、`docs`。

## 保留范围（确认为在用）
- 导航路由指向页面：`blog`/`project`/`friends`/`about`/`blog/archive`/`blog/collections`/`private`（参考 docusaurus.config.ts:55–68）
- 博客主题覆盖目录：`src/theme/**`（Docusaurus swizzle 覆盖，自动生效）
- 自定义博客插件与 API：`src/plugin/plugin-content-blog/index.js`、`api/**`、`static/js/telegram-notify.js`（`vercel.json` 映射）
- 首页与落地页模块：`src/pages/index.tsx` 以及 `src/components/landing/**`、`src/components/magicui/**`、`src/components/ui/**`

## 识别方法（只读检查，不改动）
- 代码引用检查：检索组件/页面名的 `import` 与使用位置；仅在自身文件出现且无其他引用，视为未用。
- 路由与侧边栏检查：依据 `docusaurus.config.ts` 的导航项与 `sidebars.ts` 的文档导航，保留对应页面与文档。
- 主题覆盖保留：`src/theme` 下文件即使无显式 import，也可能被主题自动加载，默认保留。

## 初步候选删除清单（检测到无引用）
- `src/components/YourComponent.tsx`（无引用）
- `src/components/DiagnosticTool.tsx`（仅自身命中）
- `src/components/BrowserWindow/index.tsx`（仅自身命中）
- `src/components/CodeSandBox/index.tsx`（无引用）
- 搜索相关（当前未启用，且未在主题中使用）：
  - `src/components/BlogSearchResults/index.tsx`
  - `src/components/SearchBox/index.tsx`
  - `src/components/SearchHighlighter/index.tsx`
  - `src/utils/fuseSearch.ts`
- `src/components/Tweet/index.tsx`（仅与 `src/css/tweet-theme.css` 互相关联，无页面使用）
- 重复/冗余配置与资源：
  - `sidebars.js`（配置使用 `sidebars.ts`）
  - `i18n/en/**`（当前 locales 仅 `zh-CN`）
  - `mock-data/**`（仅在说明文档中提及，不参与运行时）
  - `.github/workflows/docsearch.yml.bak`（备份文件）

说明：`src/theme/**`、`api/**`、`data/**`、`docs/**`、`src/pages/**` 中的导航指向页面与博客集合页均保留。`

## 实施步骤
- 步骤 1：按上表逐项删除候选文件/目录。
- 步骤 2：全局引用校验：运行 `pnpm build`，确保无导入错误、类型错误与路由 404。
- 步骤 3：本地验证：`pnpm start`，人工回归导航（博客、项目、友链、关于、归档、系列、私密页）。
- 步骤 4：静态资源核对：确保首页与项目页图片/资源仍可加载。
- 步骤 5：提交清理：`git add -A && git commit -m "chore: remove unused pages/components"`

## 验证与回归要点
- 构建：`pnpm build` 通过且无警告（或仅非阻塞警告）。
- 运行：重要路由均能打开；博文页的密码封装与社交分享正常（`src/theme/BlogPostPage/index.tsx` 有引用）。
- API：天气/地理/位置/每日语录接口在本地与 Vercel 映射下可用（参考 `src/utils/api-helpers.ts` 与 `vercel.json`）。

## 交付物
- 一套最小化的页面/组件集，删除清单对应的变更。
- 构建与运行验证通过的清理分支。
- 简短变更说明：删除项列表与保留理由，方便后续维护。