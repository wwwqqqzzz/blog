/**
 * 调试辅助工具 - 已禁用
 */

// 初始化调试工具（已禁用）
export function initDebugTools(): void {
  // 已禁用
  console.log('Debug tools disabled')
}

// 修补webpack错误处理程序（已禁用）
export function patchWebpackErrorHandler(): void {
  // 已禁用
}
