diff --git a/node_modules/webpack-dev-server/client/overlay.js b/node_modules/webpack-dev-server/client/overlay.js
index 5c5e1b2..e3a9c9c 100644
--- a/node_modules/webpack-dev-server/client/overlay.js
+++ b/node_modules/webpack-dev-server/client/overlay.js
@@ -248,7 +248,9 @@ function createOverlay(type, message, iframe) {
 }
 
 function handleError(error, iframe) {
-  createOverlay("error", error, iframe);
+  // 禁用错误覆盖层
+  console.warn("[Webpack Overlay Disabled] Error suppressed:", error);
+  // createOverlay("error", error, iframe);
 }
 
 function handleWarning(warning, iframe) {
@@ -256,7 +258,9 @@ function handleWarning(warning, iframe) {
 }
 
 function handleWarnings(warnings, iframe) {
-  createOverlay("warning", warnings, iframe);
+  // 禁用警告覆盖层
+  console.warn("[Webpack Overlay Disabled] Warnings suppressed:", warnings);
+  // createOverlay("warning", warnings, iframe);
 }
 
 export { formatProblem, createOverlay, handleError, handleWarning, handleWarnings };
